---
driver:
  name: vagrant
  provider:
    name: virtualbox
platforms:
  - name: bullseye64
    box: debian/bullseye64
  - name: buster64
    box: debian/buster64
provisioner:
  name: ansible
  become: true
lint: yamllint .
